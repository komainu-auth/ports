[package]
name = "komainu_ports"
version = "0.2.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
komainu_domain = { git = "ssh://git@github.com/komainu-auth/domain.git", tag = "v0.3.0" }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
